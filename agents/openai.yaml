interface:
  display_name: "Agent Governor"
  short_description: "Control agent routing, cost, and permissions"
  default_prompt: "Use $agent-governor to route this work under the economy-first cost policy."
policy:
  allow_implicit_invocation: true
